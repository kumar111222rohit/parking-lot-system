 // the registration number for two cars can never be same is assumed
class Car {
    constructor (NUMBER, COLOR) {
        this.NUMBER = NUMBER;
        this.COLOR = COLOR;
    }
}

module.exports = Car;
